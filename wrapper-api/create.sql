create table encrypted_person (age integer, churn integer, credit_card integer, credit_score integer, estimated_salary float(53), is_active_member integer, products_number integer, tenure integer, balance bigint, id bigserial not null, country varchar(255), gender varchar(255), signature varchar(255), primary key (id));
create table encrypted_person (age integer, churn integer, credit_card integer, credit_score integer, estimated_salary float(53), is_active_member integer, products_number integer, tenure integer, balance bigint, id bigserial not null, country varchar(255), gender varchar(255), signature varchar(255), primary key (id));
